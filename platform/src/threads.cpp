#include <cstddef>

#include <aleph/platform.hpp>

#include "os_dependent.hpp"

namespace aleph::platform {
    auto bindThread(std::uint32_t numa_node, std::uint32_t core_id) noexcept -> bool {

    }
}  // namespace aleph::platform::topology